# Grafana 仪表盘自动装配:从容器内 /var/lib/grafana/dashboards 加载 JSON。
apiVersion: 1
providers:
  - name: synapse
    orgId: 1
    folder: Synapse
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
